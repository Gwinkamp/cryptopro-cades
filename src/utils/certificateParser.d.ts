import type { Certificate } from '../Certificate';
/**
 * Парсит данные сертификата.
 * @param certificate Сертификат.
 * @throws {CryptoError} в случае ошибки.
 */
export declare function parseCertificate(certificate: Certificate): void;
