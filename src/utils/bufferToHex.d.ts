/**
 * Конвертировать массив байт в строкове 16-ричное представление.
 * @param buffer массив байт.
 * @returns строка в 16-ричном представлении.
 */
export declare function bufferToHex(buffer: ArrayBuffer): string | null;
